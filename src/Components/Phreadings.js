import React from 'react';
import { Typography } from '@material-ui/core';

const Phreadings = () => {

    // Pull ph info and display it

    return (
        <div>
            <Typography variant="h4">
                Ph 14
            </Typography>
        </div>
    );
};


export default Phreadings;